# Diwan AI Studio

Prototype web app for Arabic-first poetry typesetting with RTL editing, structured verse layouts, live print preview, DOCX import, local draft storage, smart layout suggestion, and PDF export.

## Run

Open `index.html` directly or serve the folder:

```bash
python3 -m http.server 4173
```

Then browse `http://localhost:4173`.
